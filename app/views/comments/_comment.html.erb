<% cache comment do %>
  <div class="comment">
    <h3>Comment <%= comment.id %>:</h3>
    <p><%= comment.body %></p>
    <div class="reply">
      <h4>Reply <%= comment.replies.first.id %>:</h4>
      <p><%= comment.replies.first.body %></p>
    </div>
  </div>
<% end %>
